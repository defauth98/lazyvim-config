return {
  "saghen/blink.cmp",
  opts = {
    sources = {
      default = { "lsp", "path", "snippets" },
    },
    completion = {
      ghost_text = { enabled = false }, -- Desativa o ghost text no blink
    },
  },
}
